# 🐍 נחשון - שפת תכנות עברית

<div dir="rtl">

## ברוכים הבאים לנחשון!

**נחשון** היא שפת תכנות מבוססת פייתון עם תחביר עברי מלא.
השפה נועדה להנגיש תכנות לדוברי עברית, במיוחד למתחילים ותלמידים.

נחשון מאפשרת לכתוב קוד בעברית טהורה, כולל מילות מפתח, שמות משתנים ופונקציות מובנות.

---

## ✨ תכונות

- 🔤 **תחביר עברי מלא** - כל מילות המפתח בעברית
- 🐍 **מבוסס פייתון** - תחביר מוכר ונוח
- 📚 **ספרייה סטנדרטית בעברית** - פונקציות מובנות בעברית
- ⚡ **קל לשימוש** - התקנה והרצה פשוטות
- 🛠️ **CLI מלא** - הרצה, בנייה ומצב אינטראקטיבי
- 📝 **הודעות שגיאה בעברית** - קל להבין מה השתבש

---

## 🚀 התחלה מהירה

### התקנה

**מ-PyPI (הכי פשוט):**
```bash
pip install nachshon
```

**מקוד מקור:**
```bash
# שכפול הפרויקט
git clone https://github.com/nachshon-lang/nachshon.git
cd nachshon

# התקנה במצב פיתוח
pip install -e .
```

### הוספת nachshon ל-PATH

לאחר ההתקנה, הוסף את תיקיית הסקריפטים של פייתון ל-PATH שלך:

**macOS/Linux:**
```bash
# הוסף לקובץ ~/.zshrc או ~/.bashrc
export PATH="/Library/Frameworks/Python.framework/Versions/3.11/bin:$PATH"

# או אם משתמש ב-pip עם --user
export PATH="$HOME/.local/bin:$PATH"

# טען מחדש
source ~/.zshrc  # או source ~/.bashrc
```

**Windows:**
```powershell
# הוסף את תיקיית Scripts של Python ל-PATH
# בדרך כלל: C:\Users\USERNAME\AppData\Local\Programs\Python\Python311\Scripts
```

### הרצה ראשונה

```bash
# הדרך הכי פשוטה - פשוט תן את הקובץ!
nachshon examples/01_שלום_עולם.נח

# או עם python3 ישירות מתיקיית הפרויקט
python3 nachshon.py examples/01_שלום_עולם.נח

# או כמודול
python3 -m nachshon examples/01_שלום_עולם.נח
```

---

## 📖 שימוש

### הרצת קובץ נחשון:
```bash
nachshon examples/01_שלום_עולם.נח
# או
nachshon run examples/01_שלום_עולם.נח
```

### בניית קובץ פייתון:
```bash
nachshon build examples/01_שלום_עולם.נח
```

### מצב אינטראקטיבי (REPL):
```bash
nachshon repl
# או פשוט
nachshon
```

### בדיקת תחביר:
```bash
nachshon check examples/01_שלום_עולם.נח
```

---

## 📖 מילות מפתח

### בקרת זרימה

| עברית | Python | תיאור |
|-------|--------|-------|
| `אם` | `if` | תנאי |
| `אחרת` | `else` | אחרת |
| `אחרת_אם` | `elif` | אחרת אם |
| `בעוד` / `כל_עוד` | `while` | לולאת while |
| `עבור` | `for` | לולאת for |
| `בתוך` | `in` | בתוך (לולאה) |
| `לא_בתוך` | `not in` | לא בתוך |

### פונקציות ומחלקות

| עברית | Python | תיאור |
|-------|--------|-------|
| `הגדר` | `def` | הגדרת פונקציה |
| `החזר` | `return` | החזרת ערך |
| `הפסק` | `break` | הפסקת לולאה |
| `המשך` | `continue` | המשך לאיטרציה הבאה |
| `עבור_הלאה` | `pass` | לא לעשות כלום |
| `מחלקה` | `class` | הגדרת מחלקה |
| `למבדה` / `פונקציה_אנונימית` | `lambda` | פונקציה אנונימית |
| `הנב` | `yield` | גנרטור |

### ייבוא

| עברית | Python | תיאור |
|-------|--------|-------|
| `ייבא` | `import` | ייבוא מודול |
| `מתוך` | `from` | ייבוא מתוך |
| `בתור` | `as` | שינוי שם |

### טיפול בשגיאות

| עברית | Python | תיאור |
|-------|--------|-------|
| `נסה` | `try` | נסה בלוק קוד |
| `תפוס` | `except` | תפוס שגיאה |
| `לבסוף` | `finally` | הרץ לבסוף |
| `זרוק` | `raise` | זרוק שגיאה |
| `עם` | `with` | מנהל הקשר |
| `וודא` | `assert` | וידוא תנאי |

### ערכים מיוחדים

| עברית | Python | תיאור |
|-------|--------|-------|
| `אמת` | `True` | ערך אמת |
| `שקר` | `False` | ערך שקר |
| `ריק` | `None` | ערך ריק |

### אופרטורים לוגיים

| עברית | Python | תיאור |
|-------|--------|-------|
| `וגם` | `and` | וגם |
| `או` | `or` | או |
| `לא` | `not` | לא (שלילה) |
| `גלובלי` | `global` | משתנה גלובלי |

---

## 📚 פונקציות מובנות

| עברית | Python | תיאור |
|-------|--------|-------|
| `הדפס()` | `print()` | הדפסה למסך |
| `קלט()` | `input()` | קריאת קלט מהמשתמש |
| `אורך()` | `len()` | אורך רשימה/מחרוזת |
| `סוג()` | `type()` | סוג משתנה |
| `טווח()` | `range()` | יצירת טווח מספרים |
| `מספר_שלם()` | `int()` | המרה למספר שלם |
| `מספר_עשרוני()` | `float()` | המרה למספר עשרוני |
| `מחרוזת()` | `str()` | המרה למחרוזת |
| `רשימה()` | `list()` | יצירת רשימה |
| `מילון()` | `dict()` | יצירת מילון |
| `קבוצה()` | `set()` | יצירת קבוצה |
| `בוליאני()` | `bool()` | המרה לבוליאני |
| `ערך_מוחלט()` | `abs()` | ערך מוחלט |
| `סכום()` | `sum()` | סכום רשימה |
| `מינימום()` | `min()` | ערך מינימלי |
| `מקסימום()` | `max()` | ערך מקסימלי |
| `ממוין()` | `sorted()` | מיון רשימה |
| `הפוך()` | `reversed()` | היפוך רשימה |
| `מספר_אינדקס()` | `enumerate()` | מספור רשימה |
| `צמד()` | `zip()` | צימוד רשימות |
| `מפה()` | `map()` | מיפוי פונקציה על רשימה |
| `סנן()` | `filter()` | סינון רשימה |
| `פתח()` | `open()` | פתיחת קובץ |

---

## 💡 דוגמאות קוד

### שלום עולם
```python
הדפס("שלום עולם!")
```

### משתנים
```python
שם = "דניאל"
גיל = 25
הדפס("שלום, " + שם)
הדפס("בן " + מחרוזת(גיל))
```

### תנאים
```python
גיל = 18

אם גיל >= 18:
    הדפס("מבוגר")
אחרת:
    הדפס("קטין")
```

### לולאות
```python
# לולאת עבור
עבור i בתוך טווח(5):
    הדפס(i)

# לולאת בעוד
מונה = 0
בעוד מונה < 5:
    הדפס(מונה)
    מונה = מונה + 1
```

### פונקציות
```python
הגדר ברכה(שם):
    הדפס("שלום, " + שם + "!")
    החזר אמת

ברכה("יוסי")

הגדר חיבור(א, ב):
    החזר א + ב

תוצאה = חיבור(3, 5)
הדפס(תוצאה)  # 8
```

### רשימות
```python
פירות = ["תפוח", "בננה", "תפוז"]

עבור פרי בתוך פירות:
    הדפס(פרי)

הדפס("יש " + מחרוזת(אורך(פירות)) + " פירות")
```

### מילונים
```python
אדם = {"שם": "יוסי", "גיל": 30}

הדפס(אדם["שם"])
הדפס(אדם["גיל"])

אדם["עיר"] = "תל אביב"
```

### פונקציה רקורסיבית
```python
הגדר עצרת(נ):
    אם נ <= 1:
        החזר 1
    החזר נ * עצרת(נ - 1)

הדפס(עצרת(5))  # 120
```

---

## 📁 מבנה הפרויקט

```
nachshon/
├── nachshon/
│   ├── __init__.py
│   ├── __main__.py   # נקודת כניסה למודול
│   ├── lexer.py      # מנתח לקסיקלי
│   ├── parser.py     # מנתח תחבירי
│   ├── transpiler.py # ממיר לפייתון
│   ├── loader.py     # טוען מודולים נחשון
│   └── cli.py        # ממשק שורת פקודה
├── examples/         # 28 דוגמאות קוד
│   ├── 01_שלום_עולם.נח
│   ├── 02_משתנים.נח
│   ├── ...
│   ├── 27_יוניקוד_ועברית.נח
│   ├── 28_מיני_פרויקטים.נח
│   └── lib/          # ספריות לדוגמה
│       └── מתמטיקה.נח
├── tests/           # בדיקות יחידה
│   ├── test_lexer.py
│   ├── test_parser.py
│   ├── test_transpiler.py
│   └── test_cli.py
├── vscode-extension/ # תוסף VS Code
│   └── ...          # תמיכה מלאה ב-RTL
└── README.md
```

---

## 🖥️ תוסף VS Code

הפרויקט כולל תוסף VS Code עם תמיכה מלאה ב-RTL!

### תכונות התוסף:
- ✅ עריכה נכונה מימין לשמאל (RTL)
- ✅ הדגשת תחביר מלאה
- ✅ הרצת קבצים ישירות מהעורך
- ✅ תמיכה בקבצי `.נח` ו-`.nach`

### התקנה:
```bash
cd vscode-extension
npm install
npm run compile
npm run package
# התקן את קובץ ה-.vsix מתוך VS Code
```

למידע נוסף ראו [vscode-extension/README.md](vscode-extension/README.md)

---

## 🧪 הרצת בדיקות

```bash
# כל הבדיקות
python -m pytest tests/ -v

# בדיקות ספציפיות
python -m pytest tests/test_lexer.py -v
python -m pytest tests/test_parser.py -v
python -m pytest tests/test_transpiler.py -v
```

או:

```bash
python tests/test_lexer.py
python tests/test_parser.py
python tests/test_transpiler.py
python tests/test_cli.py
```

---

## 🔧 פקודות CLI

| פקודה | תיאור |
|-------|-------|
| `nachshon <file.נח>` | הרצת קובץ נחשון (ישירות) |
| `nachshon run <file>` | הרצת קובץ נחשון |
| `nachshon build <file>` | יצירת קובץ פייתון |
| `nachshon repl` | מצב אינטראקטיבי |
| `nachshon check <file>` | בדיקת תחביר |
| `nachshon tokens <file>` | הצגת טוקנים (דיבאג) |
| `nachshon ast <file>` | הצגת עץ תחביר (דיבאג) |
| `nachshon version` | הצגת גרסה |

### אפשרויות נוספות

```bash
# הצגת קוד פייתון לפני הרצה
nachshon run --show-python examples/01_שלום_עולם.נח

# בנייה לקובץ ספציפי
nachshon build examples/01_שלום_עולם.נח -o output.py

# הצגת גרסה
nachshon --version
```

---

## 🤝 תרומה לפרויקט

מוזמנים לתרום! ניתן:
- לדווח על באגים
- להציע תכונות חדשות
- לשפר את התיעוד
- להוסיף דוגמאות קוד

---

## 📜 רישיון

MIT License

---

## 🙏 תודות

- לקהילת הפייתון הישראלית
- לכל התורמים והמשתמשים

---

**נחשון** - כי תכנות צריך להיות בשפה שלך! 🇮🇱

</div>
