# דוגמה 3: תנאים
# Example 3: Conditionals

גיל = 18

אם גיל < 13:
    הדפס("ילד")
אחרת_אם גיל < 18:
    הדפס("נער")
אחרת_אם גיל < 65:
    הדפס("מבוגר")
אחרת:
    הדפס("אזרח ותיק")

# בדיקת מספר זוגי או אי-זוגי
מספר = 7

אם מספר % 2 == 0:
    הדפס(מחרוזת(מספר) + " הוא מספר זוגי")
אחרת:
    הדפס(מחרוזת(מספר) + " הוא מספר אי-זוגי")

# בדיקה עם אופרטורים לוגיים
ציון = 85

אם ציון >= 90 וגם ציון <= 100:
    הדפס("מצוין!")
אחרת_אם ציון >= 80 וגם ציון < 90:
    הדפס("טוב מאוד!")
אחרת_אם ציון >= 70 וגם ציון < 80:
    הדפס("טוב")
אחרת_אם ציון >= 60 וגם ציון < 70:
    הדפס("מספיק")
אחרת:
    הדפס("נכשל")
