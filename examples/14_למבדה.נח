# דוגמה 14: פונקציות אנונימיות (למבדה)
# Example 14: Lambda functions

# פונקציה אנונימית בסיסית - כפל במספר
כפול = פונקציה_אנונימית x: x * 2
הדפס("כפול של 5:", כפול(5))

# פונקציה אנונימית עם מספר פרמטרים
חבר = פונקציה_אנונימית א, ב: א + ב
הדפס("5 + 3 =", חבר(5, 3))

# שימוש בלמבדה עם מפה
מספרים = [1, 2, 3, 4, 5]
ריבועים = רשימה(מפה(פונקציה_אנונימית x: x * x, מספרים))
הדפס("ריבועים:", ריבועים)

# שימוש בלמבדה עם סנן
זוגיים = רשימה(סנן(פונקציה_אנונימית x: x % 2 == 0, מספרים))
הדפס("זוגיים:", זוגיים)

# שימוש בלמבדה עם ממוין
מילים = ["תפוח", "בננה", "גזר"]
ממוין_לפי_אורך = ממוין(מילים, key=פונקציה_אנונימית מ: אורך(מ))
הדפס("ממוין לפי אורך:", ממוין_לפי_אורך)

# פונקציה שמחזירה למבדה
הגדר יוצר_מכפיל(מכפיל):
    החזר פונקציה_אנונימית x: x * מכפיל

כפול3 = יוצר_מכפיל(3)
הדפס("כפול 3 של 7:", כפול3(7))
