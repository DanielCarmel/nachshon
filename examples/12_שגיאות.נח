# דוגמה 12: טיפול בשגיאות
# Example 12: Exception Handling

הגדר חלק_בטוח(א, ב):
    נסה:
        תוצאה = א / ב
        החזר תוצאה
    תפוס:
        הדפס("שגיאה: לא ניתן לחלק!")
        החזר ריק

# בדיקות
הדפס("10 / 2 = " + מחרוזת(חלק_בטוח(10, 2)))
הדפס("10 / 0 = " + מחרוזת(חלק_בטוח(10, 0)))
