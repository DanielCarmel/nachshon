# דוגמה 13: משפט עם (with)
# Example 13: With Statement

# יצירת קובץ לכתיבה
עם פתח("test_output.txt", "w") בתור קובץ:
    קובץ.write("שלום מנחשון!")

# קריאה מקובץ
עם פתח("test_output.txt", "r") בתור קובץ:
    תוכן = קובץ.read()
    הדפס("תוכן הקובץ: " + תוכן)

הדפס("הקובץ נסגר אוטומטית!")
