# ╔══════════════════════════════════════════════════════════════╗
# ║  דוגמה 5: פונקציות - הבסיס לקוד מודולרי                      ║
# ║  Example 5: Functions - The Foundation of Modular Code       ║
# ╚══════════════════════════════════════════════════════════════╝

# ===== פונקציה פשוטה ללא פרמטרים =====
הגדר ברכה():
    """מדפיסה ברכה פשוטה"""
    הדפס("🌟 שלום וברכה!")

ברכה()

# ===== פונקציה עם פרמטר =====
הגדר ברך_אישית(שם):
    """מברכת אדם בשמו"""
    הדפס("👋 שלום, " + שם + "!")

ברך_אישית("יוסי")
ברך_אישית("רחל")

# ===== פונקציה שמחזירה ערך =====
הגדר כפול(מספר):
    """מחזירה את הכפול של המספר"""
    החזר מספר * 2

תוצאה = כפול(5)
הדפס("כפול 5 = " + מחרוזת(תוצאה))

# ===== פונקציה עם מספר פרמטרים =====
הגדר חשב_שטח_מלבן(אורך, רוחב):
    """מחשבת שטח מלבן"""
    החזר אורך * רוחב

שטח = חשב_שטח_מלבן(5, 3)
הדפס("שטח מלבן 5x3 = " + מחרוזת(שטח))

# ===== פונקציה עם ערך ברירת מחדל =====
הגדר חזקה(בסיס, מעריך):
    """מחשבת חזקה"""
    אם מעריך == 0:
        החזר 1
    תוצאה = 1
    עבור _ בתוך טווח(מעריך):
        תוצאה = תוצאה * בסיס
    החזר תוצאה

הדפס("2^10 = " + מחרוזת(חזקה(2, 10)))

# ===== פונקציה רקורסיבית - עצרת =====
הגדר עצרת(נ):
    """מחשבת עצרת (n!) באופן רקורסיבי"""
    אם נ <= 1:
        החזר 1
    החזר נ * עצרת(נ - 1)

הדפס("\n📊 טבלת עצרות:")
עבור מ בתוך טווח(1, 8):
    הדפס(מחרוזת(מ) + "! = " + מחרוזת(עצרת(מ)))

# ===== פונקציה רקורסיבית - פיבונאצ'י =====
הגדר פיבונאצי(נ):
    """מחזירה את האיבר ה-n בסדרת פיבונאצ'י"""
    אם נ <= 1:
        החזר נ
    החזר פיבונאצי(נ - 1) + פיבונאצי(נ - 2)

הדפס("\n📊 סדרת פיבונאצ'י (10 איברים ראשונים):")
סדרה = []
עבור א בתוך טווח(10):
    סדרה = סדרה + [פיבונאצי(א)]
הדפס(מחרוזת(סדרה))

# ===== פונקציה שמחזירה מספר ערכים =====
הגדר סטטיסטיקה(מספרים):
    """מחזירה סכום, ממוצע, מינימום ומקסימום"""
    סה_כ = 0
    עבור מ בתוך מספרים:
        סה_כ = סה_כ + מ
    
    ממוצע = סה_כ / אורך(מספרים)
    מינ = מספרים[0]
    מקס = מספרים[0]
    
    עבור מ בתוך מספרים:
        אם מ < מינ:
            מינ = מ
        אם מ > מקס:
            מקס = מ
    
    החזר [סה_כ, ממוצע, מינ, מקס]

נתונים = [4, 8, 15, 16, 23, 42]
תוצאות = סטטיסטיקה(נתונים)
הדפס("\n📈 סטטיסטיקה עבור " + מחרוזת(נתונים) + ":")
הדפס("  סכום: " + מחרוזת(תוצאות[0]))
הדפס("  ממוצע: " + מחרוזת(תוצאות[1]))
הדפס("  מינימום: " + מחרוזת(תוצאות[2]))
הדפס("  מקסימום: " + מחרוזת(תוצאות[3]))

# ===== פונקציה מסדר גבוה - מקבלת פונקציה כפרמטר =====
הגדר החל_על_כל(רשימה, פעולה):
    """מחילה פונקציה על כל איבר ברשימה"""
    תוצאה = []
    עבור איבר בתוך רשימה:
        תוצאה = תוצאה + [פעולה(איבר)]
    החזר תוצאה

הגדר ריבוע(איקס):
    החזר איקס * איקס

מספרים = [1, 2, 3, 4, 5]
ריבועים = החל_על_כל(מספרים, ריבוע)
הדפס("\nריבועים של " + מחרוזת(מספרים) + " = " + מחרוזת(ריבועים))

# ===== פונקציה - בדיקת ראשוניות =====
הגדר האם_ראשוני(נ):
    """בודקת אם מספר הוא ראשוני"""
    אם נ < 2:
        החזר שקר
    אם נ == 2:
        החזר אמת
    אם נ % 2 == 0:
        החזר שקר
    
    מ = 3
    בעוד מ * מ <= נ:
        אם נ % מ == 0:
            החזר שקר
        מ = מ + 2
    החזר אמת

הדפס("\n🔢 מספרים ראשוניים עד 50:")
ראשוניים = []
עבור מ בתוך טווח(2, 51):
    אם האם_ראשוני(מ):
        ראשוניים = ראשוניים + [מ]
הדפס(מחרוזת(ראשוניים))

# ===== פונקציה - מיון בועות =====
הגדר מיון_בועות(רשימה):
    """ממיינת רשימה בשיטת מיון בועות"""
    נ = אורך(רשימה)
    עבור א בתוך טווח(נ):
        עבור ב בתוך טווח(0, נ - א - 1):
            אם רשימה[ב] > רשימה[ב + 1]:
                # החלפה
                זמני = רשימה[ב]
                רשימה[ב] = רשימה[ב + 1]
                רשימה[ב + 1] = זמני
    החזר רשימה

לא_ממוין = [64, 34, 25, 12, 22, 11, 90]
הדפס("\nלפני מיון: " + מחרוזת(לא_ממוין))
ממוין = מיון_בועות(לא_ממוין)
הדפס("אחרי מיון: " + מחרוזת(ממוין))

