# דוגמה 4: לולאות
# Example 4: Loops

# לולאת עבור עם טווח
הדפס("ספירה מ-1 עד 5:")
עבור i בתוך טווח(1, 6):
    הדפס(i)

# לולאת עבור על רשימה
הדפס("\nפירות:")
פירות = ["תפוח", "בננה", "תפוז", "ענב"]
עבור פרי בתוך פירות:
    הדפס("- " + פרי)

# לולאת בעוד
הדפס("\nספירה לאחור:")
מונה = 5
בעוד מונה > 0:
    הדפס(מונה)
    מונה = מונה - 1
הדפס("שיגור! 🚀")

# לולאה עם הפסק
הדפס("\nחיפוש מספר 5:")
עבור מספר בתוך טווח(1, 10):
    אם מספר == 5:
        הדפס("מצאתי את 5!")
        הפסק
    הדפס(מספר)

# לולאה עם המשך
הדפס("\nמספרים זוגיים בלבד:")
עבור מספר בתוך טווח(1, 11):
    אם מספר % 2 != 0:
        המשך
    הדפס(מספר)
