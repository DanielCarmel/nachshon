# דוגמה 15: הבנת רשימות (List Comprehensions)
# Example 15: List comprehensions

# הבנת רשימה בסיסית - ריבועים
מספרים = [1, 2, 3, 4, 5]
ריבועים = [x * x עבור x בתוך מספרים]
הדפס("ריבועים:", ריבועים)

# הבנת רשימה עם תנאי - מספרים זוגיים בלבד
זוגיים = [x עבור x בתוך מספרים אם x % 2 == 0]
הדפס("זוגיים:", זוגיים)

# ריבועים של זוגיים בלבד
ריבועי_זוגיים = [x * x עבור x בתוך מספרים אם x % 2 == 0]
הדפס("ריבועי זוגיים:", ריבועי_זוגיים)

# הכפלת מחרוזות
מילים = ["שלום", "עולם", "נחשון"]
גדולות = [מ.upper() עבור מ בתוך מילים]
הדפס("אותיות גדולות:", גדולות)

# סינון מילים ארוכות
ארוכות = [מ עבור מ בתוך מילים אם אורך(מ) > 4]
הדפס("מילים ארוכות:", ארוכות)

# שימוש עם טווח
עשרה_ראשונים = [x + 1 עבור x בתוך טווח(10)]
הדפס("עשרה ראשונים:", עשרה_ראשונים)

# כפולות של 3 עד 30
כפולות_3 = [x עבור x בתוך טווח(31) אם x % 3 == 0]
הדפס("כפולות של 3:", כפולות_3)
